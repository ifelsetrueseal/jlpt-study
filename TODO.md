# 백로그

지금은 안 하기로 한 것들. 필요해지면 그때 꺼낸다.

## 학습 추이 그래프

최근 7일 일별 학습 장수 막대. 한 번 만들었다가 홈이 길어져서 뺐다.
되살리려면 `f97cf3a` 커밋의 `WeekBars`(components/stats.tsx)와
`dailyCounts`(lib/stats.ts, 테스트 포함)를 가져오면 된다.

- 7일보다 30일 히트맵이 꾸준함을 보기엔 더 나을 수 있다
- 홈 대신 별도 통계 화면으로 빼는 쪽이 맞을지도

## 세션별 기록 목록

"9월 20일 · 한자 · 18/20 · 4분" 식의 지난 세션 목록. 진도·누적 통계와
겹쳐서 뺐다. 세션 하나하나를 되짚고 싶어지면 다시 넣는다.

## 단어·한자 목록 화면

덱 전체를 훑어보고 검색하는 화면. 지금은 세션 안에서만 카드를 본다.
덱이 수백 개를 넘어가면 필요해진다.

## Supabase 전환

지금은 `data/*.json` + localStorage. DB 로 옮길 때 갈아끼울 지점은 두 곳:

- `lib/decks.ts` — 덱 데이터 로딩
- `lib/storage.ts` — 진행도·메모·히스토리·설정

insta_admin 과 같은 방식(`@supabase/supabase-js` + `lib/db.ts` 싱글톤,
`supabase/schema.sql` 에 raw SQL)으로 맞춘다.

## 그 밖에

- 복습 단계 분포 그래프 (`stageCounts` 는 이미 구현·테스트됨, 화면만 없음)
- 단어 예문 손보기 — 215개 전부 직접 쓴 문장이라 어색한 게 섞여 있을 수 있다
